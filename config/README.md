# Config

| File | Purpose |
|------|---------|
| `mexc_keys.json` | MEXC API credentials (not committed to git — copy from `mexc_keys.example.json`) |
| `mexc_keys.example.json` | Template for API key file |
| `protobuf_setup_status.json` | Written by `mexc_proto_auto_setup.py` after protobuf setup |
